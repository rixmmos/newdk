# Per-category baseline for check-sql-injection.sh.
# Ratchet: CI fails if ANY category exceeds its number here.
# Lower it when sites are removed; never raise it silently.
# Regenerate with: dkrixserver/scripts/check-sql-injection.sh --update
format=67
stream=23
splice=123
total=213
